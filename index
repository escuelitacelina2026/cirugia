<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cirugía Astral Cristal Arcturiano</title>
    <style>
        :root { 
            --oro: #b4975a; 
            --fondo: #0a0b10; 
            --caja: #0f111a; 
            --cyan: #00bcd4; 
        }
        
        body { 
            background-color: var(--fondo); color: #ffffff; 
            font-family: 'Segoe UI', sans-serif; margin: 0; padding: 0;
            overflow-x: hidden; 
        }

        /* LOGIN STYLE */
        #login-screen { 
            position: fixed; top: 0; left: 0; width: 100%; height: 100%; 
            background: #000; display: flex; justify-content: center; align-items: center; z-index: 99999; 
        }
        .login-box { 
            border: 1px solid var(--oro); padding: 30px; border-radius: 15px; 
            width: 85%; max-width: 350px; background: #0d0d0d; text-align: center;
        }
        .password-container { position: relative; width: 100%; }
        input { display: block; width: 100%; box-sizing: border-box; margin: 15px 0; padding: 12px; background: #000; border: 1px solid #333; color: #fff; text-align: center; border-radius: 5px; }
        
        /* El "Cristal" para ver contraseña */
        .toggle-pass { 
            position: absolute; right: 10px; top: 50%; transform: translateY(-50%);
            cursor: pointer; color: var(--cyan); font-size: 18px; 
        }

        .btn-ingresar { background: var(--oro); color: #000; border: none; padding: 14px; width: 100%; cursor: pointer; font-weight: bold; border-radius: 5px; text-transform: uppercase; }

        /* ERROR ALIEN */
        #error-msg { display: none; color: #ff4444; margin-top: 15px; font-size: 13px; }
        .alien-img { width: 80px; margin-top: 10px; filter: drop-shadow(0 0 10px var(--cyan)); }

        #aula-completa { display: none; }

        header { padding: 40px 15px; text-align: center; }
        header h1 { color: var(--oro); font-size: 24px; letter-spacing: 2px; font-family: serif; }

        nav { display: flex; overflow-x: auto; gap: 10px; padding: 15px; white-space: nowrap; border-bottom: 1px solid #222; }
        nav button { background: rgba(255,255,255,0.03); border: 1px solid #333; color: #888; padding: 10px 20px; cursor: pointer; border-radius: 4px; font-size: 11px; text-transform: uppercase; }
        nav button.active { border-color: var(--oro); color: var(--oro); background: rgba(180, 151, 90, 0.1); }

        .aula-content { max-width: 800px; margin: 20px auto; background: var(--caja); padding: 25px; border-radius: 12px; width: 90%; }
        .tab-content { display: none; line-height: 1.6; }
        .tab-content.active { display: block; }

        /* VIDEO DRIVE */
        .video-container { margin-top: 40px; text-align: center; }
        .drive-frame { 
            width: 100%; max-width: 300px; aspect-ratio: 9/16; 
            border: 2px solid var(--oro); border-radius: 15px; 
        }

        .btn-download { 
            display: inline-block; background: var(--cyan); color: #000; 
            padding: 15px 25px; text-decoration: none; border-radius: 5px; 
            font-weight: bold; margin-top: 20px; 
        }
    </style>
</head>
<body>

    <div id="login-screen">
        <div class="login-box">
            <h1 style="color: var(--oro); font-size: 20px; margin: 0;">CIRUGÍA ASTRAL</h1>
            <p style="color: var(--cyan); font-size: 10px; letter-spacing: 3px;">CRISTAL ARCTURIANO</p>
            
            <input type="text" id="user" placeholder="Usuario de Nave">
            
            <div class="password-container">
                <input type="password" id="pass" placeholder="Código de Acceso">
                <span class="toggle-pass" onclick="toggleVisibility()">💎</span>
            </div>

            <button class="btn-ingresar" onclick="checkAccess()">INICIAR SECUENCIA</button>

            <div id="error-msg">
                <img src="https://cdn-icons-png.flaticon.com/512/1946/1946465.png" class="alien-img"><br>
                ALGO SALIÓ MAL. <br> COMUNÍCATE CON EL COMANDANTE.
            </div>
        </div>
    </div>

    <div id="aula-completa">
        <header>
            <h1>CIRUGÍA ASTRAL ARCTURIANA</h1>
        </header>

        <nav>
            <button onclick="showTab('acerca', this)" class="active">Acerca de</button>
            <button onclick="showTab('espacio', this)">Espacio de Sanación</button>
            <button onclick="showTab('simbolos', this)">Simbología</button>
            <button onclick="showTab('videos', this)">Videos</button>
            <button onclick="showTab('oraculo', this)">Oráculo</button>
        </nav>

        <div class="aula-content">
            <div id="acerca" class="tab-content active">
                <h2 style="color:var(--oro)">Acerca de la Cirugía Astral</h2>
                <p>Esta técnica canalizada utiliza la vibración de los cristales arcturianos para realizar sanaciones profundas en el cuerpo etérico...</p>
                </div>

            <div id="videos" class="tab-content">
                <h2 style="text-align:center">Protocolos en Video</h2>
                
                <div class="video-container">
                    <p style="color:var(--oro)">1. Técnica Cirugía Astral</p>
                    <div style="border: 1px solid #333; padding: 50px;">[Link de Youtube Oculto Aquí]</div>
                </div>

                <div class="video-container">
                    <p style="color:var(--oro)">2. Sintonización</p>
                    <iframe src="https://drive.google.com/file/d/1hI271pPlIsBm_0SE279prsdvxaTsWexf/preview" class="drive-frame" allow="autoplay"></iframe>
                </div>
            </div>

            <div id="oraculo" class="tab-content" style="text-align:center">
                <h2 style="color:var(--oro)">Oráculo Arcturiano</h2>
                <p>Utiliza estas cartas para recibir mensajes cósmicos. Recomendamos imprimir en gramaje 325 para una mejor experiencia táctil.</p>
                <a href="URL_DE_TU_PDF_AQUI" class="btn-download" target="_blank">DESCARGAR ORÁCULO PDF</a>
            </div>
        </div>
    </div>

    <script>
        function toggleVisibility() {
            var p = document.getElementById("pass");
            p.type = (p.type === "password") ? "text" : "password";
        }

        function checkAccess() {
            var u = document.getElementById('user').value.toLowerCase().trim();
            var p = document.getElementById('pass').value.trim();
            if(u === "cirugiaastral" && p === "Cristal") {
                document.getElementById('login-screen').style.display = 'none';
                document.getElementById('aula-completa').style.display = 'block';
            } else {
                document.getElementById('error-msg').style.display = 'block';
            }
        }

        function showTab(id, btn) {
            var tabs = document.getElementsByClassName('tab-content');
            for (var i = 0; i < tabs.length; i++) tabs[i].classList.remove('active');
            var btns = document.querySelectorAll('nav button');
            for (var i = 0; i < btns.length; i++) btns[i].classList.remove('active');
            document.getElementById(id).classList.add('active');
            btn.classList.add('active');
        }
    </script>
</body>
</html>
